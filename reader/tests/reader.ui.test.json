{
  "name": "reader smoke — page renders + Add Feed form",
  "aglet": "..",
  "steps": [
    { "action": "view.waitfor",
      "select": { "role": "page" },
      "timeout": 3000,
      "expect": { "ok": true, "data": { "found": true } } }
  ]
}

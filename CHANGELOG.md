# Changelog

## 0.1.0

Inaugural public release. 4 built-in aglets:

`hn` · `gh-prs` · `gh-actions` · `tokstat`

- Every app validates, ships i18n (en / zh / ja), and has scenario tests.
- Declarative manifests on the unified `jobs[]` scheduling model.
- Published to `registry.aglet.dev`.
